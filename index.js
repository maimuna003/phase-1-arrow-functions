// function expression that divides two numbers.
function divide(a, b) {
    return a / b;
  }
  
  // square arrow function that multiplies a number by itself
  let a = 2;
  const square = (a) => {
    return a * a;
  };
  square(a);
  
  // add arrow function that adds two numbers
  const add = (a, b) => {
    return a + b;
  };
  let c = 7;
  let d = 0;
  
  add(7, 0);